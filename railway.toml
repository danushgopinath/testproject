[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build --workspace=packages/shared && cd apps/api && npm run build"

[deploy]
startCommand = "cd apps/api && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
